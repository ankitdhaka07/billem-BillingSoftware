<UserControl x:Class="UI.Features.Ledger.CustomerLedgerPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="#FFF2F4F7">
        <StackPanel HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Width="420">

            <TextBlock Text="Customer Ledger"
                       FontSize="24"
                       FontWeight="Bold"
                       Foreground="#FF222222"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,6"/>
            <TextBlock Text="Generate a PDF ledger showing bills and payments"
                       FontSize="12"
                       Foreground="#FF777777"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,30"/>

            <Border Background="White"
                    CornerRadius="12"
                    Padding="24"
                    BorderBrush="#FFE0E0E0"
                    BorderThickness="1"
                    Margin="0,0,0,16">
                <StackPanel>

                    <!-- Customer -->
                    <TextBlock Text="Customer *" FontSize="12" Foreground="#FF777777" Margin="0,0,0,4"/>
                    <Border BorderBrush="#FFE0E0E0" BorderThickness="1" CornerRadius="8" Margin="0,0,0,16">
                        <ComboBox ItemsSource="{Binding Customers}"
                                  SelectedItem="{Binding SelectedCustomer, Mode=TwoWay}"
                                  DisplayMemberPath="Name"
                                  BorderThickness="0"
                                  Padding="8,6"
                                  FontSize="13"
                                  Background="Transparent"/>
                    </Border>

                    <!-- Date Range -->
                    <Grid Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="From Date *" FontSize="12" Foreground="#FF777777" Margin="0,0,0,4"/>
                            <Border BorderBrush="#FFE0E0E0" BorderThickness="1" CornerRadius="8">
                                <DatePicker SelectedDate="{Binding FromDate, Mode=TwoWay}"
                                            BorderThickness="0"
                                            Padding="6,4"
                                            FontSize="13"
                                            Background="Transparent"/>
                            </Border>
                        </StackPanel>

                        <TextBlock Grid.Column="1"
                                   Text="—"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Bottom"
                                   Margin="0,0,0,8"
                                   Foreground="#FF999999"/>

                        <StackPanel Grid.Column="2">
                            <TextBlock Text="To Date *" FontSize="12" Foreground="#FF777777" Margin="0,0,0,4"/>
                            <Border BorderBrush="#FFE0E0E0" BorderThickness="1" CornerRadius="8">
                                <DatePicker SelectedDate="{Binding ToDate, Mode=TwoWay}"
                                            BorderThickness="0"
                                            Padding="6,4"
                                            FontSize="13"
                                            Background="Transparent"/>
                            </Border>
                        </StackPanel>
                    </Grid>

                    <!-- Generate Button -->
                    <Button Content="Generate PDF Ledger"
                            Command="{Binding GenerateLedgerCommand}"
                            Height="40"
                            FontSize="14"
                            Foreground="White"
                            Background="#FF4A6CF7"
                            BorderThickness="0">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    CornerRadius="8"
                                                    Padding="12,0">
                                                <ContentPresenter HorizontalAlignment="Center"
                                                                  VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsEnabled" Value="False">
                                                    <Setter Property="Background" Value="#FFCCCCCC"/>
                                                </Trigger>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#FF3A5CE7"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>

                    <TextBlock Text="{Binding StatusMessage}"
                               FontSize="12"
                               Foreground="#FF22A06B"
                               HorizontalAlignment="Center"
                               Margin="0,12,0,0"
                               TextWrapping="Wrap"/>
                </StackPanel>
            </Border>

            <Button Content="← Back"
                    Command="{Binding BackCommand}"
                    HorizontalAlignment="Left"
                    Width="100"
                    Height="34"
                    FontSize="13"
                    Foreground="#FF555555"
                    Background="#FFF2F4F7"
                    BorderThickness="0">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="8"
                                            Padding="12,0">
                                        <ContentPresenter HorizontalAlignment="Center"
                                                          VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#FFE5E7EB"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>

        </StackPanel>
    </Grid>
</UserControl>
