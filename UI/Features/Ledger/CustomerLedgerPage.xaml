<UserControl x:Class="UI.Features.Ledger.CustomerLedgerPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="{StaticResource AppBackgroundBrush}">
        <StackPanel HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Width="440">

            <!-- Icon header -->
            <Border Width="56" Height="56"
                    CornerRadius="16"
                    Background="{StaticResource ChipRoseBrush}"
                    HorizontalAlignment="Center"
                    Margin="0,0,0,12">
                <TextBlock Text="📒" FontSize="24"
                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>

            <TextBlock Text="Customer Ledger"
                       Style="{StaticResource PageTitleText}"
                       HorizontalAlignment="Center"/>
            <TextBlock Text="View a customer's ledger of bills and payments"
                       Style="{StaticResource PageSubtitleText}"
                       HorizontalAlignment="Center"
                       Margin="0,4,0,26"/>

            <Border Style="{StaticResource Card}" Padding="26" Margin="0,0,0,16">
                <StackPanel>

                    <!-- Customer -->
                    <TextBlock Text="Customer *" Style="{StaticResource FieldLabelText}"/>
                    <ComboBox ItemsSource="{Binding Customers}"
                              SelectedItem="{Binding SelectedCustomer, Mode=TwoWay}"
                              DisplayMemberPath="Name"
                              Margin="0,0,0,14"/>

                    <!-- Date range -->
                    <Grid Margin="0,0,0,14">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="14"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="From Date *" Style="{StaticResource FieldLabelText}"/>
                            <DatePicker SelectedDate="{Binding FromDate, Mode=TwoWay}"/>
                        </StackPanel>

                        <StackPanel Grid.Column="2">
                            <TextBlock Text="To Date *" Style="{StaticResource FieldLabelText}"/>
                            <DatePicker SelectedDate="{Binding ToDate, Mode=TwoWay}"/>
                        </StackPanel>
                    </Grid>

                    <!-- PDF toggle -->
                    <CheckBox IsChecked="{Binding AlsoGeneratePdf, Mode=TwoWay}"
                              Margin="2,0,0,18"
                              Content="Also generate PDF (saved to Desktop)"/>

                    <!-- View button -->
                    <Button Content="View Ledger"
                            Command="{Binding ViewLedgerCommand}"
                            Height="40"
                            Style="{StaticResource PrimaryButton}"/>

                    <TextBlock Text="{Binding StatusMessage}"
                               FontSize="12"
                               Foreground="{StaticResource SuccessBrush}"
                               HorizontalAlignment="Center"
                               Margin="0,12,0,0"
                               TextWrapping="Wrap"/>
                </StackPanel>
            </Border>

            <Button Content="←  Back"
                    Command="{Binding BackCommand}"
                    HorizontalAlignment="Center"
                    MinWidth="110"
                    Style="{StaticResource GhostButton}"/>

        </StackPanel>
    </Grid>
</UserControl>
