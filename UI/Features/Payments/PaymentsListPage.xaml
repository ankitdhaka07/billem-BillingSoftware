<UserControl x:Class="UI.Features.Payments.PaymentsListPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="{StaticResource AppBackgroundBrush}">
        <Grid Margin="24,20,24,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Margin="2,0,0,16">
                <TextBlock Text="Payments" Style="{StaticResource PageTitleText}"/>
                <TextBlock Text="All recorded client payments"
                           Style="{StaticResource PageSubtitleText}"
                           Margin="0,4,0,0"/>
            </StackPanel>

            <!-- List card -->
            <Border Grid.Row="1" Style="{StaticResource Card}" Padding="16">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Card title + filter -->
                    <Grid Grid.Row="0" Margin="4,0,0,14">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Border Style="{StaticResource SectionAccent}"
                                    Background="{StaticResource SuccessBrush}"/>
                            <TextBlock Text="All Payments" Style="{StaticResource SectionHeaderText}" Margin="0"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="Customer"
                                       FontSize="12"
                                       Foreground="{StaticResource TextSecondaryBrush}"
                                       VerticalAlignment="Center"
                                       Margin="0,0,8,0"/>
                            <ComboBox ItemsSource="{Binding CustomerFilters}"
                                      SelectedItem="{Binding SelectedCustomerFilter}"
                                      DisplayMemberPath="Name"
                                      Width="170"
                                      Height="32"
                                      FontSize="12"/>
                            <Button Content="Clear"
                                    Command="{Binding ClearFilterCommand}"
                                    Margin="8,0,0,0"
                                    Height="32"
                                    FontSize="11.5"
                                    Style="{StaticResource SubtleButton}"/>
                        </StackPanel>
                    </Grid>

                    <!-- Column headers -->
                    <Grid Grid.Row="1" Margin="10,0,10,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="1.5*"/>
                            <ColumnDefinition Width="1.5*"/>
                            <ColumnDefinition Width="70"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="CUSTOMER" Style="{StaticResource TableHeaderText}"/>
                        <TextBlock Grid.Column="1" Text="AMOUNT" Style="{StaticResource TableHeaderText}"/>
                        <TextBlock Grid.Column="2" Text="DATE" Style="{StaticResource TableHeaderText}"/>
                    </Grid>

                    <!-- Rows -->
                    <ListView Grid.Row="2"
                              ItemsSource="{Binding Payments}"
                              BorderThickness="0"
                              Background="Transparent"
                              ItemContainerStyle="{StaticResource CardListItem}"
                              ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="2*"/>
                                        <ColumnDefinition Width="1.5*"/>
                                        <ColumnDefinition Width="1.5*"/>
                                        <ColumnDefinition Width="70"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0"
                                               Text="{Binding CustomerName}"
                                               FontSize="13"
                                               FontWeight="SemiBold"
                                               Foreground="{StaticResource TextPrimaryBrush}"
                                               VerticalAlignment="Center"/>

                                    <TextBlock Grid.Column="1"
                                               FontSize="13"
                                               FontWeight="SemiBold"
                                               Foreground="{StaticResource SuccessBrush}"
                                               VerticalAlignment="Center">
                                        <Run Text="₹"/><Run Text="{Binding AmountPaid, Mode=OneWay, StringFormat={}{0:N2}}"/>
                                    </TextBlock>

                                    <TextBlock Grid.Column="2"
                                               Text="{Binding PaymentDate, StringFormat={}{0:dd MMM yyyy}}"
                                               FontSize="12"
                                               Foreground="{StaticResource TextMutedBrush}"
                                               VerticalAlignment="Center"/>

                                    <Button Grid.Column="3"
                                            Content="View →"
                                            Style="{StaticResource LinkButton}"
                                            Command="{Binding DataContext.ViewPaymentCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                            CommandParameter="{Binding}"
                                            VerticalAlignment="Center"
                                            HorizontalAlignment="Center"/>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
            </Border>

            <!-- Footer -->
            <Button Grid.Row="2"
                    Content="←  Back"
                    Command="{Binding BackCommand}"
                    HorizontalAlignment="Left"
                    MinWidth="100"
                    Margin="0,16,0,0"
                    Style="{StaticResource GhostButton}"/>
        </Grid>
    </Grid>
</UserControl>
