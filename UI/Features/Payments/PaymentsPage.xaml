<UserControl x:Class="UI.Features.Payments.PaymentsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="#FFF2F4F7">

        <StackPanel HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Width="400">

            <TextBlock Text="Payments"
                       FontSize="22"
                       FontWeight="SemiBold"
                       Foreground="#FF333333"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,6"/>

            <TextBlock Text="Record and track client payments"
                       FontSize="12"
                       Foreground="#FF999999"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,32"/>

            <!-- Record a Payment -->
            <Button Command="{Binding RecordPaymentCommand}"
                    Margin="0,0,0,14"
                    Height="64"
                    Style="{DynamicResource HomeTileStyle}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="💰" FontSize="22" VerticalAlignment="Center" Margin="0,0,12,0"/>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Record a Payment" FontSize="15" FontWeight="SemiBold"/>
                        <TextBlock Text="Log a new payment received from a client" FontSize="11" Foreground="#FF777777"/>
                    </StackPanel>
                </StackPanel>
            </Button>

            <!-- View All Payments -->
            <Button Command="{Binding ViewPaymentsCommand}"
                    Margin="0,0,0,14"
                    Height="64"
                    Style="{DynamicResource HomeTileStyle}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="📜" FontSize="22" VerticalAlignment="Center" Margin="0,0,12,0"/>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="View All Payments" FontSize="15" FontWeight="SemiBold"/>
                        <TextBlock Text="Browse and filter all recorded transactions" FontSize="11" Foreground="#FF777777"/>
                    </StackPanel>
                </StackPanel>
            </Button>

            <!-- Back -->
            <Button Command="{Binding BackCommand}"
                    Content="← Back to Home"
                    HorizontalAlignment="Center"
                    Margin="0,10,0,0"
                    Width="160"
                    Height="34"
                    FontSize="13"
                    Foreground="#FF555555"
                    Background="Transparent"
                    BorderThickness="0"
                    Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="8"
                                            Padding="12,0">
                                        <ContentPresenter HorizontalAlignment="Center"
                                                          VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#FFE5E7EB"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>

        </StackPanel>
    </Grid>
</UserControl>
