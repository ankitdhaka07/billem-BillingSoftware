<UserControl x:Class="UI.Features.Bills.CheckBillsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="{StaticResource AppBackgroundBrush}">
        <Grid Margin="24,20,24,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Margin="2,0,0,16">
                <TextBlock Text="Bills" Style="{StaticResource PageTitleText}"/>
                <TextBlock Text="Browse previously generated invoices"
                           Style="{StaticResource PageSubtitleText}"
                           Margin="0,4,0,0"/>
            </StackPanel>

            <!-- Bills list -->
            <Border Grid.Row="1" Style="{StaticResource Card}" Padding="16">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Card title + customer filter -->
                    <Grid Grid.Row="0" Margin="4,0,0,14">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Border Style="{StaticResource SectionAccent}"/>
                            <TextBlock Text="All Bills" Style="{StaticResource SectionHeaderText}" Margin="0"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="Customer"
                                       FontSize="12"
                                       Foreground="{StaticResource TextSecondaryBrush}"
                                       VerticalAlignment="Center"
                                       Margin="0,0,8,0"/>
                            <ComboBox ItemsSource="{Binding CustomerFilters}"
                                      SelectedItem="{Binding SelectedCustomerFilter}"
                                      DisplayMemberPath="Name"
                                      Width="170"
                                      Height="32"
                                      FontSize="12"/>
                            <Button Content="Clear"
                                    Command="{Binding ClearFilterCommand}"
                                    Margin="8,0,0,0"
                                    Height="32"
                                    FontSize="11.5"
                                    Style="{StaticResource SubtleButton}"/>
                        </StackPanel>
                    </Grid>

                    <!-- Column headers -->
                    <Grid Grid.Row="1" Margin="10,0,10,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="1.5*"/>
                            <ColumnDefinition Width="1.5*"/>
                            <ColumnDefinition Width="70"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="INVOICE #" Style="{StaticResource TableHeaderText}"/>
                        <TextBlock Grid.Column="1" Text="CUSTOMER" Style="{StaticResource TableHeaderText}"/>
                        <TextBlock Grid.Column="2" Text="DATE" Style="{StaticResource TableHeaderText}"/>
                        <TextBlock Grid.Column="3" Text="TOTAL" Style="{StaticResource TableHeaderText}"/>
                    </Grid>

                    <ListView Grid.Row="2"
                              ItemsSource="{Binding Bills}"
                              BorderThickness="0"
                              Background="Transparent"
                              ItemContainerStyle="{StaticResource CardListItem}"
                              ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="2*"/>
                                        <ColumnDefinition Width="2*"/>
                                        <ColumnDefinition Width="1.5*"/>
                                        <ColumnDefinition Width="1.5*"/>
                                        <ColumnDefinition Width="70"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0"
                                               Text="{Binding InvoiceNumber}"
                                               FontWeight="SemiBold"
                                               FontSize="13"
                                               Foreground="{StaticResource TextPrimaryBrush}"
                                               VerticalAlignment="Center"/>

                                    <TextBlock Grid.Column="1"
                                               Text="{Binding Customer.Name}"
                                               FontSize="13"
                                               Foreground="{StaticResource TextSecondaryBrush}"
                                               VerticalAlignment="Center"/>

                                    <TextBlock Grid.Column="2"
                                               Text="{Binding Date, StringFormat={}{0:dd MMM yyyy}}"
                                               FontSize="12"
                                               Foreground="{StaticResource TextMutedBrush}"
                                               VerticalAlignment="Center"/>

                                    <TextBlock Grid.Column="3"
                                               FontSize="13"
                                               FontWeight="SemiBold"
                                               Foreground="{StaticResource SuccessBrush}"
                                               VerticalAlignment="Center">
                                        <Run Text="₹"/><Run Text="{Binding TotalAmount, Mode=OneWay, StringFormat={}{0:N2}}"/>
                                    </TextBlock>

                                    <Button Grid.Column="4"
                                            Content="View →"
                                            Style="{StaticResource LinkButton}"
                                            Command="{Binding DataContext.ViewBillCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                            CommandParameter="{Binding}"
                                            VerticalAlignment="Center"
                                            HorizontalAlignment="Center"/>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
            </Border>

            <!-- Footer -->
            <Button Grid.Row="2"
                    Content="←  Back"
                    Command="{Binding BackCommand}"
                    HorizontalAlignment="Left"
                    MinWidth="100"
                    Margin="0,16,0,0"
                    Style="{StaticResource GhostButton}"/>
        </Grid>
    </Grid>
</UserControl>
